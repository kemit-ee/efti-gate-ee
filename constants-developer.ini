[DSL]
PORT=8088
# The developer mock has no dependencies — no RESQL_URL, no RUUTER_URL, no core. Every route
# answers from a canned literal. This keeps the publicly exposed container unable to reach any
# real service even if a route were mis-authored.
