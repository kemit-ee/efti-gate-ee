declaration:
  description: "Readiness probe for the developer mock. No dependencies — the mock holds no state
    and talks to nothing, so if the process is up it is ready."

respond:
  return: "OK"
  status: 200
  wrapper: false
  next: end
