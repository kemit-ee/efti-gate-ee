# Public — no auth required (callback, logout, dev-login).

declaration:
  description: "auth/POST guard — public. No credential required (callback, logout, dev-login)."

guard_success:
  return: "success"
  status: 200
  wrapper: false
