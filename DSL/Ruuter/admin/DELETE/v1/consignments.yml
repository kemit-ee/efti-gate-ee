delete_consignment:
  call: http.post
  args:
    url: "[#RESQL_URL]/efti/soft_delete_consignment"
    headers:
      Content-Type: "application/json"
    body:
      datasetId: "${incoming.params.pathParams[0]}"
  result: delete_result
  next: check_delete

# soft_delete_consignment RETURNS the tombstone row (row_id, dataset_id, status, created_at);
# an empty body means no such consignment. No re-fetch needed.
check_delete:
  switch:
    - condition: ${delete_result.response.body == null || delete_result.response.body.length == 0}
      next: not_found
    - condition: ${delete_result.response.body[0].status != 'DELETED'}
      next: delete_failed
  next: respond

respond:
  return: ""
  status: 204
  wrapper: false
  next: end

delete_failed:
  return: {"error": "Consignment deletion could not be verified"}
  status: 500
  wrapper: false
  next: end

not_found:
  return: {"error": "Not Found"}
  status: 404
  wrapper: false
  next: end
