declaration:
  description: "Send follow-up message to local platform — shared by eDelivery and Authority API"

setup:
  assign:
    requestId: ${incoming.headers['x-request-id']}

insert_log:
  call: http.post
  args:
    url: "[#RESQL_URL]/efti/insert_follow_up_log"
    headers:
      Content-Type: "application/json"
    body:
      requestingGateId: "${incoming.body.uil.gateId}"
      datasetRequestId: "${incoming.body.uil.datasetId}"
      destinationGateId: "${incoming.body.uil.gateId}"
      destinationPlatformId: "${incoming.body.uil.platformId}"
      payload: "${incoming.body.message}"
      status: "DELIVERED"
  result: log_result

check_insert:
  switch:
    - condition: ${log_result.response.status >= 400 || !log_result.response.body.length}
      next: insert_failed

get_platform:
  call: http.post
  args:
    url: "[#RESQL_URL]/efti/get_platform_by_id"
    headers:
      Content-Type: "application/json"
    body:
      id: "${incoming.body.uil.platformId}"
  result: platform_result

check_platform:
  switch:
    - condition: ${!platform_result.response.body.length}
      next: platform_not_found
    - condition: ${!!platform_result.response.body[0]?.eDeliveryCert}
      next: request_to_xml

forward_to_rest_platform:
  call: http.post
  args:
    url: "${platform_result.response.body[0]?.baseUrl}/v1/dataset/${incoming.body.uil.datasetId}/follow-up"
    headers: "${Object.assign(platform_result.response.body[0]?.headers ?? {}, {'Content-Type': 'application/json', 'X-Dataset-Request-ID': incoming.body.referenceIds[0], 'x-request-id': requestId})}"
    body: ${incoming.body}
  result: platform_response
  next: respond

request_to_xml:
  call: http.post
  args:
    url: "[#XML_MAPPER_URL]/api/v1/followup/request-to-xml"
    headers:
      x-request-id: ${requestId}
      content-type: "application/json"
    body: ${incoming.body}
  result: xml_request

forward_to_edelivery_platform:
  call: http.post
  timeout: 65000
  args:
    url: "[#EDELIVERY_URL]/api/v1/send/${platform_result.response.body[0].id}"
    headers:
      content-type: "text/xml"
      x-request-id: ${requestId}
    content_type: plaintext
    body: ${xml_request.response.body}
  result: platform_response

respond:
  return: ${incoming.body.uil}
  wrapper: false
  status: ${platform_response.response.status}
  next: end

platform_not_found:
  return:
    error: "Platform Not Found"
  wrapper: false
  status: 404
  next: end

insert_failed:
  return:
    error: "Failed to log follow-up message"
  wrapper: false
  status: 500
