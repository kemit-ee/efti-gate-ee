check_db:
  call: http.post
  args:
    url: "http://resql:8090/efti/get_db_status"
    headers:
      Content-Type: "application/json"
    body: {}
  result: db_status
  next: respond

respond:
  return: ${db_status.response.body}
  next: end
