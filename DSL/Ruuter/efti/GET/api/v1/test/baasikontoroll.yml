check_db:
  call: http.post
  args:
    url: "[#RESQL_URL]/efti/get_db_status"
    headers:
      Content-Type: "application/json"
    body: {}
  result: db_status
  next: respond

respond:
  return: ${db_status.response.body}
  next: end
