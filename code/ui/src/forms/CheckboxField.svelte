<script lang="ts">
  export let label: string|undefined = undefined
  export let checked = false
  export let helpText = ''

  export let id = label ? label.replace(/\./g, '-') : ''
</script>

<div class="relative flex items-start {$$props.class ?? ''}">
  <div class="flex items-center h-5">
    <input {id} type="checkbox" bind:checked {...$$restProps} class="">
  </div>
  {#if label}
    <label for={id} class="ml-2">{label}</label>
  {/if}
  {#if helpText}<span class="help-text ml-1 cursor-default" title={helpText}>ⓘ</span>{/if}
  <slot/>
</div>

<style>
  :global(input[type=checkbox]) {
    @apply focus:ring-primary-600 h-4 w-4 text-primary-600 border-neutral-500 rounded;
  }
</style>
