port: 8087

http_codes_allow_list: [200, 201, 204, 400, 401, 403, 404, 422, 500]

dsl:
  project: xroad

guards:
  enforce_on_methods: [GET, POST, PUT, DELETE]

incoming_requests:
  allowed_method_types: [GET, POST, PUT, DELETE]

internal_requests:
  # check-xroad-client is called over this Ruuter's own loopback.
  block_private_networks: false

stop_in_case_of_exception: true

logging:
  display_request_content: false
  display_response_content: false
